name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # Run every 5 minutes
  workflow_dispatch:        # Allows manual runs

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -L --silent https://pitcherreportyakker.streamlit.app/ > /dev/null
